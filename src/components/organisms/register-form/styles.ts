import { StyleSheet } from "react-native";

export const RegisterFormStyleSheet = StyleSheet.create({
  checkbox: {
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
  },
  checkboxBold: {
    fontWeight: "bold",
  },
});
