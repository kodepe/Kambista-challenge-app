import { StyleSheet } from "react-native";

export const RegisterFormHeaderStyleSheet = StyleSheet.create({
  title: { color: "#060F26", fontSize: 24, fontWeight: "600" },
  subtitle: { textAlign: "center", fontWeight: 300 },
});
