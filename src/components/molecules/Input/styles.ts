import { StyleSheet } from "react-native";

export const InputStyleSheet = StyleSheet.create({
  inputOutline: {
    borderRadius: 12,
    backgroundColor: "#FFF",
  },

  contentStyle: {
    color: "#686868",
    fontSize: 14,
  },
});
